import { Title, Controls, Canvas, Meta, Source } from '@storybook/blocks';
import * as HeadStories from './index.stories';
import * as  Css from "./headling.css";


<Meta of={HeadStories} name="コード"/>
<Title />
<Canvas />

## CSS
<Source code={`
.header {
  color: var(--Main-Color-Base);
  font-size: 2.4rem;
  font-weight: 700;
  font-family: var(--font-family-heading);
}
`}
language="css"
/>

## JavaScript
<Source code={`
function name() {
  console.log("rete"
  );
}
name();
`}
language="jsx"
/>